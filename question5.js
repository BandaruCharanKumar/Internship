var Color = ["Red", "Green", "White", "Black"];
console.log(Color.toString()); // convert object to string
console.log(Color.join());  // convert array into a single string
console.log(Color.join("+")); // convert array into a single string with "+" in between


